{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Database": {
    "//": "Local dev runs on Sqlite so the site works with no database server installed. Set this to Postgres (and ConnectionStrings:DefaultConnection in user-secrets) to develop against the production provider.",
    "Provider": "Sqlite"
  }
}
